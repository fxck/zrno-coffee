<!-- #ZEROPS_EXTRACT_START:intro# -->
A **single-service preview/QA** environment: one `app` container running the real
production build, plus PostgreSQL, object storage, Meilisearch and Mailpit. No
dev pair, no idling — just the shipped app on the **Lightweight** plan. Pick this
to show ZRNO off or to QA a build without the dev tooling.
<!-- #ZEROPS_EXTRACT_END:intro# -->
