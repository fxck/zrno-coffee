<!-- #ZEROPS_EXTRACT_START:intro# -->
The **highly-available** production topology: a 3-node PostgreSQL with
auto-failover, larger object storage, Meilisearch, and the `app` scaled to 2–4
**dedicated-CPU** containers behind a health-gated rolling deploy (zero
downtime). Requires the **Serious** project plan. Pick this to run ZRNO in
production with no single point of failure — swap Mailpit for a real SMTP
provider and add live Stripe keys when you go live.
<!-- #ZEROPS_EXTRACT_END:intro# -->
