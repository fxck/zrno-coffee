<!-- #ZEROPS_EXTRACT_START:intro# -->
The **production** topology: highly-available 3-node PostgreSQL with
auto-failover, larger object storage, Meilisearch, and the `app` scaled to 2–4
**dedicated-CPU** containers behind a health-gated rolling deploy (zero
downtime). Requires the **Serious** project plan. Pick this to actually run ZRNO
in production — swap Mailpit for a real SMTP provider and add live Stripe keys
when you go live.
<!-- #ZEROPS_EXTRACT_END:intro# -->
