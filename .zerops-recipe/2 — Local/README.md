<!-- #ZEROPS_EXTRACT_START:intro# -->
The **local development** backing services — PostgreSQL, object storage,
Meilisearch and Mailpit, nothing else. You run the ZRNO app on your own machine
(`npm run dev`) and reach these over the Zerops VPN (`zcli vpn up`) by hostname,
so your laptop works against real cloud data services with no local Docker.
Lightweight plan. Pick this for a fast local loop backed by managed services.
<!-- #ZEROPS_EXTRACT_END:intro# -->
